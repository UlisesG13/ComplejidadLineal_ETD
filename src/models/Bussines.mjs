export class Bussines {
    name
    address
    city
    state
    postal_code
    constructor(name, address, city, state, postal_code) {
        this.name = name
        this.address = address
        this.city = city
        this.state = state
        this.postal_code = postal_code
    }
}